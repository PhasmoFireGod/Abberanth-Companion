/* ============================================================
   Abberanth Companion — Site Configuration
   Add DM email addresses to dmEmails to grant elevated access.
   ============================================================ */

const SITE_CONFIG = {
  dmEmails: [
    'phasmofiregod@yahoo.com',
  ],
};
